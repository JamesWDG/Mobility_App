import {StyleSheet} from 'react-native';

import colors from '../../config/Colors';

export const styles = StyleSheet.create({
    inputFeildsStyles: { justifyContent : "center", alignItems : "center" , borderRadius : 100,width : 60 , height : 60,borderWidth : 0 , backgroundColor : colors.c_F5F5F5}
});
